{
  "name": "interplanetary-tracker",
  "version": "1.0.0",
  "description": "Keep a registry of projects stored in [ipfs](ipfs.io) on your local machine, and publish a list of your projects and their history to ipns.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel -o bin/iptr src/index.js",
    "frontend": "watchify -t babelify -o template/app.js -- frontend/index.js"
  },
  "bin": {
    "iptr": "./bin/iptr"
  },
  "author": "Sam Marshall",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.8.1",
    "ipfs-api": "^2.3.2",
    "ramda": "^0.17.1",
    "shelljs": "^0.5.3",
    "superagent": "^1.4.0"
  },
  "devDependencies": {
    "babelify": "^6.3.0",
    "watchify": "^3.4.0"
  }
}
